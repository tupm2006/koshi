fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.28
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
python-multipart>=0.0.9
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiofiles>=23.2.0
google-auth>=2.28.0
requests>=2.31.0
alembic==1.19.1

# MySQL driver. PyMySQL is pure Python, so the slim image needs no compiler and
# no libmysqlclient headers. `cryptography` (already present via python-jose)
# is what MySQL 8's default caching_sha2_password authentication needs.
PyMySQL>=1.1.0
